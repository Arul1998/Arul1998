name: Generate profile summary cards

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:
  push:
    branches:
      - main
    paths-ignore:
      - "assets/**"
      - "profile-summary-card-output/**"

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - uses: vn7n24fzkq/github-profile-summary-cards@release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          USERNAME: ${{ github.repository_owner }}
          # London (BST); productive-time card aligns with UK local time in summer
          UTC_OFFSET: 1
          AUTO_PUSH: false

      - name: Keep only dracula theme
        run: |
          find profile-summary-card-output -mindepth 1 -maxdepth 1 -type d ! -name 'dracula' -exec rm -rf {} +
          rm -f profile-summary-card-output/README.md

      - name: Commit profile summary cards
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          git add -A profile-summary-card-output/

          if git diff --staged --quiet; then
            echo "No profile card changes detected. Skipping commit."
            exit 0
          fi

          git commit -m "Update profile summary cards [automated]"
          git pull --rebase origin main
          git push
